VUE_APP_NETEASE_API_URL=/api
VUE_APP_NETEASE_API_PROXY_TARGET=http://localhost:3000
VUE_APP_ELECTRON_API_URL=/api
VUE_APP_ELECTRON_API_URL_DEV=http://127.0.0.1:10754
# Last.fm API key is public enough for auth URL generation, but rotate it after exposure.
VUE_APP_LASTFM_API_KEY=
# Main process only. Do not expose this through Vite define or renderer code.
LASTFM_SHARED_SECRET=
VUE_APP_TOUBIEC_API_URL=https://wyapi.toubiec.cn/api
# Main process only. Do not expose this through Vite define or renderer code.
TOUBIEC_TOKEN=
DEV_SERVER_PORT=20201
